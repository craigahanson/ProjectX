@page "/version"
@using ProjectX.Blazor.Services
@using ProjectX.Api

@inject IBlazorVersionService BlazorVersionService

<h1>Version</h1>

@if (version == null)
{
    <p>
        <em>Loading...</em>
    </p>
}
else
{
    <div>@version</div>
}

@code
{
    public ApiVersion version;

    protected override async Task OnInitializedAsync()
    {
        version = await BlazorVersionService.GetAsync();
    }
}